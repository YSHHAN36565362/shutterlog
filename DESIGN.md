# DESIGN.md — Shutterlog

> 이 저장소의 **설계 기준 문서**입니다.
> 디자인이나 문구를 바꿀 때는 **이 문서를 먼저 고치고, 그다음 코드를 여기에 맞추세요.**
> 코드부터 고치면 다음 사람(= 미래의 나)이 왜 그렇게 되어 있는지 알 수 없게 됩니다.

작성: 2026-08-15 (v1)
자매 문서: `githubpage/DESIGN.md` — 메인 포트폴리오의 설계 문서. 규칙 7번(절대 규칙)은 그쪽이 원본입니다.

---

## 1. 목적과 대상

| 항목 | 내용 |
|---|---|
| 목적 | 취미(사진 · 여행) 포트폴리오. 메인 포트폴리오의 **하위 카테고리**로 붙습니다 |
| 읽는 사람 | 일본 기업 채용 담당자(일본어 우선) → 한국 기업·지인(한국어) → 그 외(영어) |
| 보여주려는 것 | ① 실제로 사진을 찍는다 ② 자기 데이터를 스스로 분석한다 ③ 그걸 코드로 만든다 |
| 배포 | GitHub Pages — https://yshhan36565362.github.io/shutterlog/ |

취미 소개가 아니라 **"자기 워크플로를 자동화한 사례"** 로 읽히도록 구성했습니다.
기능을 세 개 나열한 것이 아니라, 사진 폴더가 들어가서 포트폴리오 한 페이지가
나오기까지를 한 줄기(선별 → 분석 → 액자 → 아카이브)로 설계한 것이 요지입니다.

---

## 2. 디자인 — 메인 사이트와 같은 언어, 다른 밝기

메인 포트폴리오와 **같은 디자인 토큰**을 씁니다.

```
강조   --primary #2c98f0   --teal #2fa499
6색    #2c98f0 #ec5453 #f9bf3f #a84cb8 #2fa499 #4054b2
폰트   Quicksand(본문) + Playfair Display(제목)
위계   글자 크기가 아니라 대문자 + 자간 5px + 넓은 아래 여백
버튼   12px 대문자/자간2px, radius 2px, 1px 테두리
```

**다른 점은 배경 밝기 하나입니다.** 이쪽은 어둡습니다.

> 사진을 보여주는 사이트에서 흰 배경은 사진의 밝기를 잡아먹습니다.
> 같은 팔레트를 어두운 면 위에 올린 것이지, 새 팔레트를 만든 것이 아닙니다.
> 회색을 새로 만들지 마세요 — `--bg` / `--bg-2` / `--panel` / `--panel-2` 네 단계로 끝냅니다.

액자 도구(`frame.js`)가 캡션에 쓰는 폰트도 **의도적으로 같은 Quicksand + Playfair** 입니다.
그래야 갤러리에 걸린 사진들이 사이트 디자인의 일부처럼 보입니다.
남의 앱으로 만든 액자를 붙인 게 아니라 전체가 하나의 언어로 설계됐다는 인상이
디자인 직군 지원에서는 꽤 크게 작용합니다.

---

## 3. 지구본 — 왜 이렇게 만들었나

### 마커는 WebGL 스프라이트가 아니라 진짜 `<img>` 입니다

3D 좌표를 매 프레임 화면 좌표로 **투영**해서 그 위치에 HTML 요소를 놓습니다.

- 사진이 또렷합니다 (스프라이트는 텍스처 압축·밉맵으로 뭉갭니다)
- 둥근 모서리 · 그림자 · hover · **키보드 포커스**가 전부 CSS 로 공짜입니다
- 마커 수가 40개 남짓이라 DOM 갱신 비용이 문제되지 않습니다

### LOD 4단계

| 레벨 | 카메라 거리 | 보이는 것 |
|---|---|---|
| 0 | > 3.90 | **대표 사진 1장만** |
| 1 | 2.95 – 3.90 | 지역 (일본 / 중화권 / 유럽) |
| 2 | 2.20 – 2.95 | 여행 단위 |
| 3 | 1.46 – 2.20 | 도시 단위 |

"구체가 가장 클 때는 제일 대표 사진만, 확대하면 지역마다 사진이 보이게" 라는
요구를 이 4단계로 구현했습니다. 거리 구간을 바꿀 때는 `js/globe.js` 의 `LEVELS` 만
고치면 됩니다.

### ⚠️ 텍스처와 마커의 경도 정렬 (건드리면 지도가 90° 어긋납니다)

three.js `SphereGeometry` 의 UV 는 **u = 0.5 가 +X 를 향합니다.**
반면 마커 좌표계(`toVec`)는 **경도 0 을 +Z** 로 둡니다. 그대로 두면 90° 어긋납니다.

→ `earth.rotation.y = -Math.PI / 2` 로 메시를 돌려 맞춥니다.

**텍스처의 x 계산을 바꿔서 맞추면 안 됩니다.** 그러면 이음매가 태평양에서
아메리카 대륙 한가운데로 옮겨가 캐나다·브라질이 세로로 잘립니다.

### 국경은 런타임에 그립니다

`data/world-110m.js` 의 GeoJSON 을 브라우저 안에서 4096×2048 캔버스에 그린 뒤
구에 입힙니다. 방문국 색이 `trips.meta.json` 을 따라가야 하기 때문입니다 —
나라를 하나 추가하면 이미지를 새로 만들 필요 없이 색이 늘어납니다.

날짜변경선을 넘는 폴리곤 링은 `splitRing()` 이 잘라 줍니다. 지우면 러시아·피지가
화면을 가로지르는 줄로 그려집니다.

### 휠 스크롤

지구본 위에서 휠은 확대/축소지만, **한계에 닿으면 페이지에 스크롤을 돌려줍니다.**
완전히 가로채면 지구본에서 빠져나갈 수 없게 됩니다.

---

## 4. 도구 3종의 알고리즘 결정

### 01 선별 — 그룹핑

시간만으로 묶으면 3초 안에 다른 것을 찍은 사진까지 한 그룹이 됩니다.
**촬영 간격 + 초점거리 + 구도 해시** 세 조건을 모두 만족할 때만 묶습니다.
보조로, 시간이 3배까지 벌어져도 구도가 아주 비슷하고(dHash 거리 ≤ 5) 초점거리가
같으면 묶습니다 — 자리를 고쳐 잡고 다시 찍은 재촬영을 잡기 위해서입니다.

### 01 선별 — 선명도 (여기가 가장 틀리기 쉬운 부분)

라플라시안 분산을 그대로 쓰면 **f/1.4 로 배경을 날린 사진이 "흔들림"으로 오판됩니다.**
화면 대부분이 흐려서 전체 분산이 낮게 나오기 때문입니다. 두 가지 보정을 넣었습니다.

1. 화면을 4×4 타일로 나눠 **가장 날카로운 타일의 값만** 씁니다
2. 절대 점수를 쓰지 않고 **같은 그룹 안의 상대 순위**로만 판정합니다

이 둘 중 하나라도 빼면 체감 정확도가 눈에 띄게 떨어집니다.

### 01 선별 — 삭제하지 않습니다

브라우저에는 원본을 지울 권한이 애초에 없습니다. 그리고 그게 옳습니다.
내보내는 `.sh` 도 `rm` 이 아니라 `_to_delete/` 로 **옮기는** 스크립트입니다.
되돌릴 수 있는 쪽이 항상 낫습니다.

### 02 분석 — 표준 스톱으로 묶는 이유

f/2.83, f/2.79 같은 실측값을 그대로 세면 막대가 수십 개로 흩어져 아무것도 읽히지
않습니다. 사진가는 애초에 f/2.8 · ISO 800 · 1/125s 같은 스톱 단위로 생각하므로,
**로그 거리로 가장 가까운 표준 스톱에 붙여** 집계합니다.
눈에 보이는 축과 머릿속의 축을 맞추는 것입니다.

### 02 분석 — "실패" 의 정의

선명도 **하위 25%** 를 실패로 봅니다. 절대 기준이 아니라 **이번에 넣은 사진들 안에서의
상대 기준**입니다 — 카메라·렌즈·해상도가 바뀌면 점수의 스케일 자체가 달라지기 때문입니다.
그래서 여러 여행을 합쳐 볼 때는 `analysis/` 의 스크립트에서 기준을 다시 계산합니다.

### 03 액자 — 여백은 비율이 아니라 고정 px

"가로의 6%" 로 잡으면 가로 사진에서는 좌우가, 세로 사진에서는 상하가 넓어져
액자 테두리 폭이 사진마다 달라집니다. 실제 액자는 그렇지 않습니다.
→ **사진의 긴 변을 기준으로 한 px** 로 계산합니다 (`PAD` / `CAP`).

### 03 액자 — 워터마크는 기본적으로 사진 위가 아닙니다

액자를 쓸 때 워터마크는 캡션 줄에 들어갑니다. 사진을 가리지 않는 쪽이 거의 언제나
낫기 때문입니다. 사진 위에 얹는 경우의 "자동" 은 네 귀퉁이의 평균 밝기와 **표준편차**를
재서 가장 어둡고 **평평한** 곳을 고르고, 밝은 구석이면 글자를 검게 바꿉니다.
(밝기만 보면 하늘 대신 복잡한 나뭇가지 위에 얹히는 일이 생깁니다)

---

## 5. i18n

- 기본 **일본어**. `한국어` / `EN` 버튼으로 전환. 마지막 선택은 `localStorage`.
- 문구는 외부 JSON 이 아니라 **`js/i18n.js` 안의 객체**에 둡니다.
  fetch 가 필요 없어 CORS·네트워크 실패의 영향을 받지 않습니다.
- 데이터(여행 제목·도시명)는 `trips.meta.json` 안에 `{ja, ko, en}` 으로 들어갑니다.
- **문구를 고칠 때는 `T.ja` / `T.ko` / `T.en` 세 곳을 모두 고쳐야 합니다.**
  한 곳만 고치면 다른 언어에서 옛 문구가 남습니다.
- 언어를 바꾸면 동적으로 그린 부분(여행 카드 · 지구본 마커 라벨 · 그래프)도 다시 그립니다.

---

## 6. 🚨 절대 규칙 (githubpage/DESIGN.md 7절과 동일)

2026-08-13, 실제로 배포된 사이트가 CDN 로드 실패로 통째로 망가졌습니다.

| # | 규칙 | 이 저장소에서의 구현 |
|---|---|---|
| 1 | ES 모듈 / import map / 외부 JS CDN 금지 | 전부 일반 `<script>`. 외부 JS 0개 |
| 2 | 라이브러리는 동봉하고 **없어도 동작** | `vendor/` 에 three.js · exifr · anime.js |
| 3 | 애니메이션은 "보이는 상태로 도착" 만 | 중간에 끊겨도 화면이 깨지지 않습니다 |
| 4 | 숨김의 기본값은 "보임" | `.reveal` 은 `body.reveal-ready` 가 붙어야 숨습니다 |
| 5 | 자리표시자에도 진짜 문구 | `...` 금지 |
| 6 | 폰트 CDN 은 허용 | 실패해도 모양만 영향 |
| 7 | `?v=` 캐시 무효화 숫자를 올릴 것 | CSS/JS 를 고칠 때마다 |

추가로 이 저장소만의 규칙:

| # | 규칙 | 이유 |
|---|---|---|
| 8 | `.nojekyll` 을 지우지 말 것 | GitHub Pages 의 Jekyll 이 `_` 로 시작하는 경로를 무시합니다 |
| 9 | 데이터는 `.json` 이 아니라 `.js` 로 싣는다 | `fetch` 없이 `<script>` 로 — 로컬에서도 동작 |
| 10 | 원본 사진을 스크립트가 수정하지 않는다 | `build.py` 는 `thumbs/` 에만 씁니다 |
| 11 | 사진을 네트워크로 보내지 않는다 | 도구 3종은 `fetch`/`XHR` 를 한 번도 호출하지 않습니다 |

### 스크롤 등장의 함정 (메인 사이트와 동일)

`IntersectionObserver` 는 교차 **상태가 바뀔 때만** 콜백을 줍니다. 앵커로 한 번에
점프하면 중간 요소가 한 프레임에 지나가 영영 `opacity: 0` 으로 남습니다.
→ `main.js` 의 `sweep()` 안전망이 스크롤할 때마다 지나간 요소를 직접 드러냅니다. **지우지 마세요.**

### `[hidden]` 의 함정

`display: flex` / `grid` 는 UA 스타일시트의 `[hidden] { display: none }` 을 **이깁니다.**
숨기라고 했는데 보이는 사고가 실제로 있었습니다.
→ `style.css` 상단에 `[hidden] { display: none !important; }` 전역 규칙이 있습니다.

---

## 7. 검증 체크리스트

수정 후 아래를 모두 통과해야 합니다.

- [ ] 3개 언어 버튼이 동작하고 본문 · `<html lang>` · `document.title` 이 함께 바뀐다
- [ ] 새로고침해도 마지막 선택 언어가 유지된다
- [ ] 콘솔 에러 0건
- [ ] **`vendor/three.min.js` 를 차단해도** 여행 목록 · 도구 링크가 정상이고
      지구본 자리에 대체 화면이 나온다
- [ ] **`vendor/anime.min.js` 를 차단해도** 카운터가 최종값으로 표시된다
- [ ] OS "모션 줄이기" 에서도 콘텐츠가 전부 보인다
- [ ] 지구본을 확대하면 마커가 대표 1장 → 지역 → 여행 → 도시로 늘어난다
- [ ] 지구본 뒷면의 마커가 앞으로 새어 나오지 않는다
- [ ] 휠을 끝까지 축소하면 페이지가 정상적으로 스크롤된다
- [ ] `trip.html?id=` 에 없는 값을 넣으면 "Not found" 가 나온다 (빈 화면이 아니라)
- [ ] 갤러리 정렬 6종이 모두 동작하고, EXIF 가 없는 사진은 방향과 무관하게 뒤로 간다
- [ ] 도구 3종에 사진을 넣어도 Network 탭에 요청이 하나도 생기지 않는다
- [ ] 액자 도구에서 세로 사진과 가로 사진의 테두리 폭이 같다
- [ ] 390px / 768px / 1440px 에서 가로 스크롤이 생기지 않는다

---

## 8. 앞으로 할 것

- [ ] `data/trips.meta.json` 의 `date` / `endDate` 를 실제 날짜로 고치고 `needsReview` 를 `false` 로
- [ ] 각 여행 폴더에 베스트 컷 넣기 → `python3 build.py`
- [ ] 대표 사진(`cover`) 직접 지정
- [ ] **폰 사진의 GPS 를 촬영 시각 매칭으로 카메라 사진에 이식하는 기능** —
      카메라에는 GPS 가 없어 지도 마커가 수동 좌표에 의존하고 있습니다.
      실제 사진가들이 GPS 로거로 하는 작업이고, `build.py` 에 붙이기 좋습니다.
- [ ] 라이트룸 프리셋(.xmp) 미리보기 — WebGL 셰이더로 근사. 임팩트는 크지만 난이도도 큽니다
- [ ] 여행 계획 탭(골든아워 · 블루아워 · 달 위상) — 아카이브 안의 작은 탭으로
